<style>
  .container {
    width: 800px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    align-content: center;
  }

  .value {
    width: 15px;
    padding: 10px;
    margin: 1px;
    background-color: #8257e6;
    color: #FFF;
    border-radius: 6px;
    text-align: center;
    text-decoration: none;
  }

  .level {
    width: 350px;
    margin: 10px;
    display: flex;
    justify-content: space-between;
  }

  .answers {
    width: 350px;
    display: flex;
    justify-content: space-between
  }
</style>
<div class="container">
  <label>Olá, <strong>{{ name }}</strong>! Tudo bem?</label>
  <h3>{{ title }}</h3>

  <br />
  
  <strong>{{ description }}</strong>

  <div class="level">
    <span>Pouco provável</span>
    <span>Muito provável</span>
  </div>

  <div class="answers">
    <a class="value" href="{{ link }}/1?su_id={{ id }}" target="_blank">1</a>
    <a class="value" href="{{ link }}/2?su_id={{ id }}" target="_blank">2</a>
    <a class="value" href="{{ link }}/3?su_id={{ id }}" target="_blank">3</a>
    <a class="value" href="{{ link }}/4?su_id={{ id }}" target="_blank">4</a>
    <a class="value" href="{{ link }}/5?su_id={{ id }}" target="_blank">5</a>
    <a class="value" href="{{ link }}/6?su_id={{ id }}" target="_blank">6</a>
    <a class="value" href="{{ link }}/7?su_id={{ id }}" target="_blank">7</a>
    <a class="value" href="{{ link }}/8?su_id={{ id }}" target="_blank">8</a>
    <a class="value" href="{{ link }}/9?su_id={{ id }}" target="_blank">9</a>
    <a class="value" href="{{ link }}/10?su_id={{ id }}" target="_blank">10</a>
  </div>

  <br />
  <br />

  <strong>Sua opinião é muito importante para nós</strong>
  <h3>Equipe | <strong>NLW</strong></h3>
</div>